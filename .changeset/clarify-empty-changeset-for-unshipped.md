---
---

docs: clarify that changes shipping no package content (REFERENCES.md, docs,
tooling, gates) use an empty changeset, not a patch one — a patch there drifts
the version with a contentless release. CLAUDE.md only, ships nothing — empty
changeset.
